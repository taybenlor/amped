<% if params[:action] != "new" %>
  <li><%= link_to "Create an account", new_user_path, :class => "new_account" %></li>
<% end %>
  <li id="login_button"><%= link_to "Sign in", login_path, :id => 'sign_in_button' %></li>
  <div id="sign_in_box" class="inner_glow">
    <h2>We meet again.</h2>
    <%= render "user_sessions/signin" %>
  </div>
  <script>
  $('login_button').opensContextBox($('sign_in_box'), {direction:"bottom", buttonDistance:-1});
  </script>